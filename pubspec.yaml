name: myapp
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
   
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.7     # Compatible version for SVG rendering

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1   # Stable linting package

flutter:
  uses-material-design: true
